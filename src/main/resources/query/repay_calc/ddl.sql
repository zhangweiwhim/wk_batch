CREATE TABLE `wk_repay_calc_copy` (
  `REPAYCALCID` int(11) NOT NULL AUTO_INCREMENT COMMENT '自增列ID',
  `APPLYID` int(11) NOT NULL COMMENT '申请ID',
  `APPLYCD` varchar(20) DEFAULT NULL,
  `DUETERMS` int(11) DEFAULT NULL COMMENT '逾期期数',
  `DUEDATE` varchar(10) DEFAULT NULL COMMENT '逾期开始日期',
  `DUESEQNO` int(11) DEFAULT NULL COMMENT '逾期开始期号',
  `OVERDUEDAYS` int(11) DEFAULT NULL COMMENT '逾期天数',
  `DUESEED` double DEFAULT NULL COMMENT '逾期本金',
  `DUEINTEREST` double DEFAULT NULL COMMENT '逾期利息',
  `DUEREMAIN` double DEFAULT NULL COMMENT '逾期尾款',
  `REMAINSEED` double DEFAULT NULL COMMENT '剩余本金',
  `REMAININTEREST` double DEFAULT NULL COMMENT '剩余利息',
  `REMAINREMAIN` double DEFAULT NULL COMMENT '剩余尾款',
  `REMAINAMOUNT` double DEFAULT NULL COMMENT '剩余租金',
  `MAXDUEDATE` varchar(10) DEFAULT NULL COMMENT '最新逾期日期',
  `REMAINCOUNT` int(11) DEFAULT NULL COMMENT '剩余还款次数',
  `OVERDUEFINE` double DEFAULT NULL COMMENT '逾期罚息',
  `LEGALFEE` double DEFAULT NULL COMMENT '诉讼费用',
  `COLLECTFEE` double DEFAULT NULL COMMENT '催收费用',
  `RECEIVEREFUNDAMOUNT` double DEFAULT NULL COMMENT '截止今日应收月供',
  `REALRECEIVEREFUNDAMOUNT` double DEFAULT NULL COMMENT '截止今日实收月供',
  `LEASEHOLD` int(10) DEFAULT NULL COMMENT '已归还期数',
  `CHUCKFLG` tinyint(4) DEFAULT NULL COMMENT '卡盘FLG',
  `RECENTDEDUCTTIME` datetime DEFAULT NULL COMMENT '最近一次扣款日期',
  PRIMARY KEY (`APPLYID`),
  KEY `IDX_APPLYID` (`APPLYID`) USING BTREE,
  KEY `idx_applycd` (`APPLYCD`),
  KEY `IDX_OVERDUEDAYS` (`OVERDUEDAYS`) USING BTREE,
  KEY `IX_REPAYCALCID` (`REPAYCALCID`) USING BTREE
)